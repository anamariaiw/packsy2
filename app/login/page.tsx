"use client";
import { useState } from "react";import { supabase } from "@/lib/supabaseClient";import Link from "next/link";import { useRouter } from "next/navigation";
export default function LoginPage(){const router=useRouter();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[status,setStatus]=useState("");
async function login(){setStatus("Signing in...");const{error}=await supabase.auth.signInWithPassword({email,password});if(error){setStatus(error.message);return}router.push("/dashboard")}
async function google(){await supabase.auth.signInWithOAuth({provider:"google",options:{redirectTo:`${window.location.origin}/dashboard`}})}
return <div className="loginWrap"><div className="loginCard"><h1 className="center">Welcome to Packsy</h1><p className="center subtitle">AI packing lists saved locally and in the cloud.</p><label className="formLabel">Email</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/><label className="formLabel">Password</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button className="primary" style={{marginTop:18}} onClick={login}>Log In</button><button className="secondary" style={{width:"100%",marginTop:10}} onClick={google}>Continue with Google</button><p className="center small">{status}</p><p className="center small">No account? <Link className="link" href="/register">Create one</Link></p></div></div>}