"use client";
import { useState } from "react";import { supabase } from "@/lib/supabaseClient";import Link from "next/link";
export default function RegisterPage(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[status,setStatus]=useState("");
async function register(){setStatus("Creating account...");const{error}=await supabase.auth.signUp({email,password});if(error){setStatus(error.message);return}setStatus("Account created. Check your email if confirmation is enabled, then log in.")}
return <div className="loginWrap"><div className="loginCard"><h1 className="center">Create your Packsy account</h1><p className="center subtitle">Save trips across devices.</p><label className="formLabel">Email</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/><label className="formLabel">Password</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button className="primary" style={{marginTop:18}} onClick={register}>Register</button><p className="center small">{status}</p><p className="center small">Already have an account? <Link className="link" href="/login">Log in</Link></p></div></div>}